# Portfolio 建構準備 — 策略單元歸納

> 更新 2026-08-03｜snapshot `2026-06-22`｜自建還原價(`price_adjust.py`)
>
> 這份文件把目前**可當 portfolio 成分**的策略整理成統一規格,並給出組合配置最關鍵的
> 輸入:**成分間的相關性**。先讀第 3 節 —— 它決定了現在能不能組、該怎麼組。

## 1. 可用的策略單元

目前只有 **S19** 具備「可重複執行 + IS/OS 都測過 + 在非 bypass 價格上」三個條件。
其餘策略見 `STRATEGY_REGISTRY.md`,多數是 `blocked` 或 `rejected`。

### S19 — 籌碼確認的風險調整動能(CRM)

| 項目 | 規格 |
|---|---|
| 模組 | `chip_momentum_strategy.py` |
| 方向 | long-only |
| 訊號 | `0.5*cs_rank(ts_ir(日報酬,20)) + 0.5*cs_rank((Σ20外資+Σ20投信)/近20日均量)` |
| 閘門 | 動態 universe(近20日成交值 top100,PIT)× `trend_ok`(MA20>MA60、MA60上揚、收盤>MA60) |
| 候選池 | top300 排除 17 檔價格完整性名單 → 283 檔 |
| 組合 | 10 檔等權、20 日再平衡、MA60 出場、-15% 停損、T+1 開盤 |
| 成本 | 手續費 0.1425%(單邊)+ 證交稅 0.30%(賣出) |
| 容量 | 未測。動態 top100 的流動性門檻是唯一約束,尚未做衝擊成本估計。 |

**績效(IS/OS 70:30,OS 未參與任何參數選擇)**

| 段 | 相位 Sharpe(5 個等價執行相位) | 中位 | 最小 | 年化(中位) | MaxDD |
|---|---|---|---|---|---|
| IS | 1.695 / 1.644 / 1.540 / 1.070 / 2.014 | **1.644** | **1.070** | +37.5% | -25% ~ -30% |
| OS | 3.735 / 3.805 / 3.169 / 3.802 / 3.836 | 3.802 | 3.169 | +192% | -15% ~ -20% |

**呼叫方式**

```python
import chip_momentum_strategy as s19
panel, symbols = s19.build_panel()          # 稠密 panel(keep_non_members=True)
score = s19.build_signal(panel)             # 與 panel 同 index
picks = s19.build_picks(panel, score)       # {date: [(sid, score, name), ...]}
df = s19.evaluate(panel, symbols)           # 跑滿 5 個相位,回傳每相位一列
```

`build_picks` 的輸出可直接餵給 `backtest.backtest_portfolio(picks_by_date=...)`,
也是未來把多個策略合併成單一部位表的接點。

## 2. 風險特徵(組合配置時要知道)

- **報酬極度集中**:勝率僅 29~40%,靠賺賠比 5.7~7.3 撐住。少數幾筆大贏決定全局。
  → 對 portfolio 的意義:**單一策略的權重不能高到讓幾筆交易主導整體**,且
  回測 Sharpe 對「漏掉一筆大贏」極敏感。
- **低交易頻率**:IS 段每相位僅 54~58 筆、OS 段 12~15 筆。
  → 統計上很薄。OS 的 Sharpe 3.8 **不具統計意義**,只能說「換到未參與選擇的
  區間沒有崩掉」。
- **多頭 beta 高**:與等權 universe 的日報酬相關 0.77(見下)。這不是市場中性策略。

## 3. ⚠️ 相關性 — 這是目前最重要的發現

全期日報酬相關矩陣(419 個重疊交易日,同一組合建構規則):

|  | S19_CRM | S03_momret | 純法人流 | 純風險調整動能 | 等權universe |
|---|---|---|---|---|---|
| **S19_CRM** | 1.000 | 0.755 | **0.921** | 0.829 | 0.772 |
| **S03_momret** | 0.755 | 1.000 | 0.776 | 0.881 | 0.761 |
| **純法人流** | 0.921 | 0.776 | 1.000 | 0.782 | 0.779 |
| **純風險調整動能** | 0.829 | 0.881 | 0.782 | 1.000 | 0.761 |
| **等權universe** | 0.772 | 0.761 | 0.779 | 0.761 | 1.000 |

同期單獨績效(全期、單一相位,僅供相關性脈絡參考):

| 策略 | Sharpe | 年化 | MaxDD | 交易數 |
|---|---|---|---|---|
| S19_CRM | 2.260 | +72.8% | -25.8% | 71 |
| 純法人流 | 2.128 | +66.0% | -23.1% | 78 |
| S03_momret | 1.377 | +56.6% | -37.8% | 77 |
| 純風險調整動能 | 0.820 | +27.4% | -35.1% | 84 |

**兩個結論,都會直接影響 portfolio 設計:**

1. **現在還不能組一個有意義的多策略 portfolio。** 所有候選成分彼此相關
   0.755~0.921,且每一個與等權 universe 都是 ~0.77。它們共用同一個多頭 beta,
   合併起來只會得到「一個稍微平滑的同樣東西」,分散效益接近零。
   要真正分散,需要**方向或時間尺度不同**的成分 —— 例如反轉/均值回歸、
   不同持有期(日內或數月)、或非股票資產。

2. **S19 的 alpha 主要來自法人流量那一半。** S19 與純法人流相關 0.921,而純風險
   調整動能單獨只有 Sharpe 0.820。動能那一半的貢獻主要是**穩定性**(相位穩健度
   從「兩個相位為負」變成「五個相位全 >1」),不是報酬水準。組合時不應把
   「S19」與「法人流」當成兩個獨立成分。

## 4. 組 portfolio 前必須先解決的事

依阻擋程度排序:

1. **找到真正低相關的成分。** 這是第 3 節的直接結論,也是目前最大的缺口。
   `STRATEGY_REGISTRY.md` 裡的 `rejected` 清單顯示反轉/買弱方向已被證偽多次,
   所以下一個方向應該是**不同時間尺度**或**曝險控制**(S14 breadth-regime
   只控曝險不當買點,方向對)。
2. **候選池倖存者偏誤。** 池是當前 top300,缺歷史下市股。所有絕對績效仍是
   樂觀上界。根治需外部歷史下市清單(見 `universe_bias_audit.py`)。
3. **樣本只有一個多頭窗**(2024-07~2026-06,含一次股災)。任何權重配置在這種
   樣本上做最佳化都會過擬合。建議先用**等權或風險平價**,不要做 mean-variance
   最佳化。
4. **容量與衝擊成本未測。** 10 檔集中持有 + 動態 top100,實際可容納資金未知。

## 5. 建議的下一步

1. 用 `freeze_manifest.py` 凍結 S19 規則,以 `forward_test.py` 做 snapshot 之後的
   forward-only 驗證 —— 這是唯一能把證據等級從 `active hypothesis` 升級的路。
2. 在找到第二個**低相關**成分之前,不要急著做權重配置。與其配置四個相關 0.8 的
   策略,不如先確認 S19 在 forward 是否存活。
3. 若要立刻有「組合」形式,務實作法是**單一策略 + 曝險控制**(市場濾網 S06 /
   breadth regime S14),而不是多策略加權 —— 前者才真正降低相關性集中的風險。

## 關聯檔案

- 策略實作:`chip_momentum_strategy.py`｜報告:`outputs/CHIP_MOMENTUM_REPORT.md`
- 還原價基礎:`price_adjust.py`｜排除名單:`outputs/price_integrity_excluded.json`
- 策略台帳:`STRATEGY_REGISTRY.md`(S19 條目)｜研究鐵則:`RESEARCH_OPERATING_PROTOCOL.md`
