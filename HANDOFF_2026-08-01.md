# HANDOFF 2026-08-01 — 系統稽核/修正 + 交易規則架構 + 處置股研究起點

> 這份接手先讀。分支 `fix/system-audit-p0`(已推 origin,14 commit,超前 origin/main)。
> **下一個對話焦點:研究處置股有沒有策略** → 直接跳第 4 節。

## 0. 環境鐵則(照做)
- 一律 `.venv/bin/python`(系統 python3 太新)。資料鎖 `config.SNAPSHOT_END_DATE`(現 `2026-06-22`)。
- **主回測預設未還原價會 fail-closed raise**;要跑:`SWING_ALLOW_UNADJUSTED=1`(smoke,結果標 `integrity_bypassed`)或 `SWING_PRICE_DATASET=TaiwanStockPriceAdj`(真績效,需付費 token)。
- 完整強制機制 + 指令級流程見 `RESEARCH_OPERATING_PROTOCOL.md §9/§10`。

## 1. 本分支做了什麼(commit 由新到舊)
- **執行真實性**:漲跌停可成交性(一字漲停→跳過進場、跌停→順延出場;`config.BT_MODEL_LIMIT_LOCK` 預設開)、處置期間禁新倉(`BT_MODEL_DISPOSITION`,需處置快取)。
- **處置資料層** `twse_disposition.py` + 出關事件研究 `disposition_event_study.py`。
- **架構稽核**(4 維度×對抗式):universe PIT 閘門(池晚於快照→fail-closed)、下市殭屍出場、稀疏 panel ts_ 修正、provenance。
- **operators.py**(對齊 WorldQuant 的因果算子庫)+ **factor_scan.py**(批量掃因子)。
- **P0 稽核修正**:未還原價 fail-closed 閘門、cache 加快照戳、ma_exit 次日開盤、缺bar MTM 延用last_close、法人流量補0、ETF 排除、universe 誠實 metadata + `universe_bias_audit.py`、`freeze_manifest.py`/`forward_test.py`(clean-OOS 骨架)。
- 把未追蹤引擎/測試/池 fixture 納版控(clean clone 可重現);tests/ **44 passed**。

## 2. 系統目前「乾淨」到什麼程度
- 逐訊號 look-ahead 已乾淨:T+1 開盤進場、trailing PIT 排名、因子因果、cache 快照戳、未來池閘門、漲跌停可成交性、處置禁倉。
- **唯一系統性失真源 = 候選池倖存者**(池是當前 top300,缺歷史下市股)。已量化揭露(`universe_bias_audit.py`)、fail-closed 圍堵未來池,但**未消除**——所有絕對績效標「樂觀上界、待重驗」。根治需外部歷史下市清單重建全市場 PIT 池。
- 其他限制:未還原價(預設)、單一多頭窗(2024-2026)+ 一次股災。

## 3. 已證偽/已知無 edge(別重做)
- 買弱/接刀(S4)= 負;天真 vol 節流(S1)無用;rs/downside 防禦因子與動能冗餘;winner_dna 個股單點特徵 OS 無預測力;**出關續強 = beta 非 alpha(見下)**。
- 相對最強且跨窗穩健:**S2 族群輪動**(`sector_rotation.py`);**S3 break-retest** 最抗回檔。

---

## 4. 【下一對話焦點】處置股策略研究

### 4.1 已有的資料與工具(直接用)
- `twse_disposition.py`(**上市**):
  - `fetch_notice_history(start,end)` — TWSE 歷史**注意**(真實,`notice` 端點 startDate/endDate 有效)。
  - `fetch_current_punish()` — 當前**真實處置**(openapi,含起迄,驗證用)。
  - `build_disposition_periods(notice, trading_days)` — 由「連續3交易日注意→次日起處置10日」**推導**處置期間(derived proxy,對齊 TAIEX 日曆,PIT 安全)。
  - 快取:`_cache/disposition__ALL__<snap>.pkl`(已建 2026-06-22:822 段/362 檔)、`notice__ALL__<snap>.pkl`。
- `tpex_disposition.py`(**上櫃**,2026-08-02 補):
  - `fetch_disposal_history(start,end)` — 櫃買 `bulletin/disposal` 直接給歷史**真實處置起訖**,不需推導(品質優於上市那半邊)。
  - `fetch_attention_history(start,end)` — 上櫃歷史注意。
  - 快取:`_cache/disposition_tpex__ALL__<snap>.pkl`(已建 2026-06-22:**978 段/370 檔**)、`attention_tpex__ALL__<snap>.pkl`(12117 筆)。
  - `backtest._load_disposition_days` 自動合併兩邊;只載到單邊會警告該市場不受保護。
  - ⚠ **4.2 的事件研究是在只有上市資料時做的**,樣本結構性排除整個上櫃市場 —— 這正是 4.3 第 1 點「樣本偏大型」的成因之一。重測前請先納入上櫃。
- `disposition_event_study.py`:出關次日開盤進場的 T+1/5/20 超額研究。

### 4.2 已測 → 負面結論
**出關續強(disp_end→次日進場)= 沒有可靠 edge**:全期 T+20 超額 +3.71%(t 2.40)但 **IS 拆開就沒**(超額 +1.09%、t 0.68、勝率 44%、中位 -2%),只有 OS +9.90% = **普漲 beta**(處置股=高 beta 小型股)。見 `outputs/DISPOSITION_EVENT_STUDY.md`。

### 4.3 未測、值得在新對話試的角度(附已知坑)
1. **樣本偏誤要先修**:事件研究只用「有快取價格的股」(top300∩處置=99 檔,偏大型);**真正被處置的多是中小型冷門股**,結論可能因此失真。→ 先擴充處置股的價格抓取(FinMind `TaiwanStockPrice`,注意配額)。
2. **基準要控 size beta**:現在用 excess vs TAIEX,沒控小型股 beta → OS 的「超額」可能全是小型股 beta。改用**同期同 size/同產業配對基準**或用 `_buyhold_baseline` 的等權池,重測 IS/OS。
3. **反向/避開角度**(可能比續強有搞頭):
   - **急漲觸發處置的「峰」是不是反轉點**?測 disp_start 前後(拋物線急漲後 fade)。
   - **注意→處置升級路上**做多/避開的訊號(注意當日、cum_count 升到 2/3 時)。
   - 已知:**禁掉進處置股的進場,Sharpe 反升**(0.663→0.725)→ 「避開處置股」本身可能就是小 edge,值得單獨驗。
4. **執行約束(硬限制,別忽略)**:處置期間分盤(每5/20分)+預收款券+**停信用/不得放空** → 任何「做空處置股」或「日內處置股」策略實務上做不到;只能做**多方波段 + 出關後**,且要扣分盤流動性成本。
5. **推導處置是 proxy(偏寬)**:`build_disposition_periods` 用連續3日注意,會與官方實際有出入(over-flag);要更準可改用 `notetrans`(累計次數異常,直接列「連續二/三次」)或逐日 snapshot 當前 punish 累積(forward-only)。

### 4.4 建議的乾淨流程
凍結規則(`freeze_manifest.py`)→ 只用 IS 找、OS/forward 驗;基準控 size;絕對績效在還原價前一律標「待重驗」。守 `RESEARCH_OPERATING_PROTOCOL.md`。

## 5. 待外部資料的大工(非本分支範圍)
- survivorship-free PIT 全市場池(需歷史下市清單)。
- 還原價 `TaiwanStockPriceAdj` 全量重抓 + 重跑所有報告。
- 零股資料(TWSE `TWTC7U`/`TWT53U`,FinMind 無)——評估後 CP 值低,列探索性。
